# ── Core_API ───────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.9,<1.0.0  # required by FastAPI for File/Form (UploadFile) parsing
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
sse-starlette>=2.1.0,<4.0.0

# ── LangChain_Ecosystem ────────────────────────────────────────────────────────
# The LangChain 0.3 line has multiple published security advisories. Keep
# these integration packages on their maintained 1.x-compatible releases.
langchain>=1.3.9,<2.0.0
langchain-core>=1.2.22,<2.0.0
langchain-community>=0.4.0,<1.0.0
langchain-google-genai>=3.0.0,<5.0.0
langchain-groq>=1.0.0,<2.0.0

# ── Qdrant_Vector_Database ─────────────────────────────────────────────────────
qdrant-client>=1.9.0,<2.0.0
langchain-qdrant>=1.0.0,<2.0.0

httpx>=0.27.0,<1.0.0  # required by fastapi.testclient.TestClient

# ── Google_AI_(used by list_models.py to list available Gemini models) ─────────
# NOTE: this is the new unified `google-genai` SDK (import path `google.genai`),
# not the older `google-generativeai` package — they are different packages.
# langchain-google-genai (above) pulls in its own google-generativeai
# dependency transitively for embeddings, so it does not need to be listed
# here separately.
google-genai>=0.8.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0

# ── MySQL ──────────────────────────────────────────────────────────
mysql-connector-python>=9.0.0,<10.0.0

# ── Rate Limiting (NEW - SECURITY) ─────────────────────────────────
slowapi>=0.1.9,<0.2.0

# ── Circulars_Scraper ──────────────────────────────────────────────
beautifulsoup4>=4.12.0,<5.0.0
lxml>=5.3.0,<6.0.0
pymupdf>=1.24.0,<2.0.0
apscheduler>=3.10.0,<4.0.0

# ── Development_or_Testing ────────────────────────────────────────────────────────
pytest>=9.0.3,<10.0.0
pytest-asyncio>=1.0.0,<2.0.0

# ─────────────────────────────────────────────────────────────────
# ─────────────────────────────────────────────────────────────────
# ─────────────────────────────────────────────────────────────────