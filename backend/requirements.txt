# ── Core_API ───────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
sse-starlette>=2.1.0,<4.0.0

# ── LangChain components used by the application ─────────────────────────────
# Do not add the umbrella `langchain` or `langchain-community` packages here:
# neither is imported by this project, and they pull in LangGraph, NumPy,
# SQLAlchemy and other packages that materially increase a small service's RSS.
langchain-core>=1.2.22,<2.0.0
langchain-google-genai>=3.0.0,<5.0.0
langchain-groq>=1.0.0,<2.0.0

# ── Qdrant_Vector_Database ─────────────────────────────────────────────────────
qdrant-client>=1.9.0,<2.0.0
langchain-qdrant>=1.0.0,<2.0.0

# ── Modern_Async_HTTP_Client ─────────────────────────────────────────────────────
httpx>=0.27.0,<1.0.0

# `google-genai` is installed transitively by langchain-google-genai.
python-dotenv>=1.0.0,<2.0.0

# ── MySQL ──────────────────────────────────────────────────────────
mysql-connector-python>=9.0.0,<10.0.0

# ── Rate Limiting (NEW - SECURITY) ─────────────────────────────────
slowapi>=0.1.9,<0.2.0

# ── Admin circular uploads ─────────────────────────────────────────
# PyMuPDF is loaded only when an administrator uploads a PDF.  The scheduled
# browser scraper is deliberately kept in requirements-circular-scraper.txt
# so a normal 512 MiB Render web service never installs or imports Selenium.
pymupdf>=1.24.0,<2.0.0
# ─────────────────────────────────────────────────────────────────
